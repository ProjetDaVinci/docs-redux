export type selectedCategories = {
  animated: boolean;
  itemIndex: number;
  sectionIndex: number;
  filterName: string;
};

export type changeCategories = {
  itemIndex: number;
  sectionIndex: number;
  filterName: string;
};
